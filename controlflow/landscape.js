//function that takes the width and height of an image and checks if its landscape mode

// function isLandscape(width, height) {
//     return (width > height) ? true : false
// }

console.log(isLandscape(4, 6))
// the function below is the same as that above
function isLandscape(width, height) {
    return (width > height)
}